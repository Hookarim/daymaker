# Daymaker Environment Variables
# Kopiere diese Datei zu .env.local und fülle die Werte aus

# Supabase — von supabase.com/dashboard → Project Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJxxxxxx...

# Anthropic API Key — von console.anthropic.com (für AI-Coach + Foto-Erkennung + Food-Suche)
# Wird NUR auf der Netlify-Server-Funktion verwendet (nie im Browser exponiert)
ANTHROPIC_API_KEY=sk-ant-xxxxx
